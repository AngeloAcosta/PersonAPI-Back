'use strict';
module.exports = function setupKinshipLogic(){
  
    function IsGrandpa(person,relative){
            if(person.id && relative.id){
               if(person.kinshipType === "F"){
                    if()
               } 
            }

    }
    return {
        IsGrandpa
    }
}